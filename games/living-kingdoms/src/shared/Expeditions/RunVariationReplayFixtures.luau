--!strict

local RunVariationReplayFixtures = {
	Version = "P06-RUN-VARIATION-REPLAY-FIXTURES-v1",
	DenseContacts = table.freeze({ 8, 12, 46 }),
	QuietApproach = table.freeze({ 45, 49, 63 }),
	NoVariation = table.freeze({ 25, 26, 29 }),
}

return table.freeze(RunVariationReplayFixtures)
