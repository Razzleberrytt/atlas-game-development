--!strict

-- Deterministic composition rules for repository-owned environmental models.
-- The runtime planner turns these constraints into stable placements using the
-- world seed, Gaussian story fields, route/creek clearances, hierarchical passes,
-- and global footprint-aware packing. No gameplay authority is created here.

local EnvironmentModelPlacementConfig = {
	Version = "environment-models-v4-hierarchical-packing",
	AlgorithmVersion = "gaussian-fields-hierarchical-poisson-v2",
	Seed = 1847,
	HalfExtentStuds = 320,
	CandidateCountPerAsset = 7000,
	LandmarkPaddingStuds = 8,
	CrossFamilySpacingScale = 0.9,
	TierOrder = table.freeze({ "Anchor", "Mass", "Detail" }),
	AssetSpecs = table.freeze({
		-- Anchor silhouettes establish the environmental story before lower-frequency fill.
		table.freeze({
			idPrefix = "corruption.dead_pine",
			assetId = "env.tree.corrupted.dead_pine_a",
			field = "corruption",
			tier = "Anchor",
			count = 9,
			packingRadiusStuds = 7,
			minSpacingStuds = 34,
			minRouteDistanceStuds = 28,
			minCreekDistanceStuds = 18,
		}),
		table.freeze({
			idPrefix = "industry.mine_support",
			assetId = "env.structure.industry.mine_support_a",
			field = "industry",
			tier = "Anchor",
			count = 5,
			packingRadiusStuds = 9,
			minSpacingStuds = 42,
			minRouteDistanceStuds = 34,
			minCreekDistanceStuds = 24,
		}),
		table.freeze({
			idPrefix = "industry.broken_support",
			assetId = "env.structure.industry.broken_support_a",
			field = "industry",
			tier = "Anchor",
			count = 3,
			packingRadiusStuds = 8,
			minSpacingStuds = 48,
			minRouteDistanceStuds = 34,
			minCreekDistanceStuds = 24,
		}),
		table.freeze({
			idPrefix = "camp.torn_shelter",
			assetId = "env.camp.frontier.torn_shelter_a",
			field = "camp",
			tier = "Anchor",
			count = 3,
			packingRadiusStuds = 7,
			minSpacingStuds = 54,
			minRouteDistanceStuds = 34,
			minCreekDistanceStuds = 24,
		}),

		-- Mass assets shape mid-scale negative space after the primary silhouettes exist.
		table.freeze({
			idPrefix = "frontier.boulder",
			assetId = "env.rock.frontier.boulder_a",
			field = "frontier",
			tier = "Mass",
			count = 11,
			packingRadiusStuds = 7,
			minSpacingStuds = 38,
			minRouteDistanceStuds = 28,
			minCreekDistanceStuds = 18,
		}),
		table.freeze({
			idPrefix = "corruption.fallen_root",
			assetId = "env.root.corrupted.fallen_mass_a",
			field = "corruption",
			tier = "Mass",
			count = 7,
			packingRadiusStuds = 8,
			minSpacingStuds = 36,
			minRouteDistanceStuds = 28,
			minCreekDistanceStuds = 18,
		}),

		-- Detail assets increase local information density without establishing navigation truth.
		table.freeze({
			idPrefix = "camp.fire_ring",
			assetId = "env.camp.frontier.abandoned_fire_ring_a",
			field = "camp",
			tier = "Detail",
			count = 4,
			packingRadiusStuds = 5,
			minSpacingStuds = 55,
			minRouteDistanceStuds = 32,
			minCreekDistanceStuds = 24,
		}),
		table.freeze({
			idPrefix = "corruption.stump_cluster",
			assetId = "env.root.corrupted.stump_cluster_a",
			field = "corruption",
			tier = "Detail",
			count = 8,
			packingRadiusStuds = 5,
			minSpacingStuds = 28,
			minRouteDistanceStuds = 26,
			minCreekDistanceStuds = 16,
		}),
		table.freeze({
			idPrefix = "industry.ore_cart",
			assetId = "env.prop.industry.ore_cart_a",
			field = "industry",
			tier = "Detail",
			count = 4,
			packingRadiusStuds = 5,
			minSpacingStuds = 38,
			minRouteDistanceStuds = 30,
			minCreekDistanceStuds = 22,
		}),
		table.freeze({
			idPrefix = "frontier.supply_crates",
			assetId = "env.prop.frontier.supply_crate_stack_a",
			field = "camp",
			tier = "Detail",
			count = 5,
			packingRadiusStuds = 4,
			minSpacingStuds = 32,
			minRouteDistanceStuds = 28,
			minCreekDistanceStuds = 20,
		}),
	}),
}

return table.freeze(EnvironmentModelPlacementConfig)
