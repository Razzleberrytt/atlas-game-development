--!strict

-- Presentation-only starter registry for the Living Kingdoms environment content factory.
-- Final meshes may replace these source paths without changing semantic IDs or gameplay authority.

local Registry = {
	Version = "ENV-ASSET-KIT-v4",
	Definitions = table.freeze({
		table.freeze({
			id = "env.rock.frontier.boulder_a",
			family = "Rock",
			variantId = "frontier.boulder.a",
			biomeIds = table.freeze({ "biome.frontier", "biome.corrupted_wilderness" }),
			scaleClass = "Large",
			collisionPolicy = "Simple",
			streamingGroupId = "main_world.resources",
			qualityTiers = table.freeze({ "Minimum", "Reduced", "Full" }),
			sockets = table.freeze({}),
			sourceModelPath = "Environment/Rocks/FrontierBoulderA",
			footprintStuds = table.freeze({ x = 12, y = 8, z = 10 }),
			presentationOnly = true,
			gameplayAuthorityOwnerId = nil,
		}),
		table.freeze({
			id = "env.tree.corrupted.dead_pine_a",
			family = "DeadTree",
			variantId = "corrupted.dead_pine.a",
			biomeIds = table.freeze({ "biome.corrupted_wilderness" }),
			scaleClass = "Hero",
			collisionPolicy = "Simple",
			streamingGroupId = "main_world.resources",
			qualityTiers = table.freeze({ "Minimum", "Reduced", "Full" }),
			sockets = table.freeze({
				table.freeze({ id = "socket.vfx.canopy", kind = "VFX" }),
				table.freeze({ id = "socket.audio.creak", kind = "Audio" }),
			}),
			sourceModelPath = "Environment/Foliage/CorruptedDeadPineA",
			footprintStuds = table.freeze({ x = 10, y = 30, z = 10 }),
			presentationOnly = true,
			gameplayAuthorityOwnerId = nil,
		}),
		table.freeze({
			id = "env.root.corrupted.fallen_mass_a",
			family = "Root",
			variantId = "corrupted.fallen_root_mass.a",
			biomeIds = table.freeze({ "biome.corrupted_wilderness", "biome.frontier" }),
			scaleClass = "Large",
			collisionPolicy = "Simple",
			streamingGroupId = "main_world.resources",
			qualityTiers = table.freeze({ "Minimum", "Reduced", "Full" }),
			sockets = table.freeze({}),
			sourceModelPath = "Environment/Foliage/FallenRootMassA",
			footprintStuds = table.freeze({ x = 14, y = 5, z = 11 }),
			presentationOnly = true,
			gameplayAuthorityOwnerId = nil,
		}),
		table.freeze({
			id = "env.root.corrupted.stump_cluster_a",
			family = "Root",
			variantId = "corrupted.stump_cluster.a",
			biomeIds = table.freeze({ "biome.corrupted_wilderness" }),
			scaleClass = "Medium",
			collisionPolicy = "Simple",
			streamingGroupId = "main_world.resources",
			qualityTiers = table.freeze({ "Minimum", "Reduced", "Full" }),
			sockets = table.freeze({}),
			sourceModelPath = "Environment/Foliage/CorruptedStumpClusterA",
			footprintStuds = table.freeze({ x = 8, y = 8, z = 8 }),
			presentationOnly = true,
			gameplayAuthorityOwnerId = nil,
		}),
		table.freeze({
			id = "env.structure.industry.mine_support_a",
			family = "Industrial",
			variantId = "mine.support.a",
			biomeIds = table.freeze({ "biome.abandoned_industry", "biome.mine" }),
			scaleClass = "Large",
			collisionPolicy = "Blocker",
			streamingGroupId = "main_world.structures",
			qualityTiers = table.freeze({ "Minimum", "Reduced", "Full" }),
			sockets = table.freeze({
				table.freeze({ id = "socket.snap.left", kind = "Snap" }),
				table.freeze({ id = "socket.snap.right", kind = "Snap" }),
				table.freeze({ id = "socket.prop.lantern", kind = "Prop" }),
			}),
			sourceModelPath = "Environment/Industry/MineSupportA",
			footprintStuds = table.freeze({ x = 16, y = 16, z = 8 }),
			presentationOnly = true,
			gameplayAuthorityOwnerId = nil,
		}),
		table.freeze({
			id = "env.structure.industry.broken_support_a",
			family = "Industrial",
			variantId = "mine.broken_support.a",
			biomeIds = table.freeze({ "biome.abandoned_industry", "biome.mine" }),
			scaleClass = "Large",
			collisionPolicy = "Blocker",
			streamingGroupId = "main_world.structures",
			qualityTiers = table.freeze({ "Minimum", "Reduced", "Full" }),
			sockets = table.freeze({}),
			sourceModelPath = "Environment/Industry/BrokenSupportA",
			footprintStuds = table.freeze({ x = 14, y = 11, z = 6 }),
			presentationOnly = true,
			gameplayAuthorityOwnerId = nil,
		}),
		table.freeze({
			id = "env.prop.industry.ore_cart_a",
			family = "Prop",
			variantId = "mine.ore_cart.a",
			biomeIds = table.freeze({ "biome.abandoned_industry", "biome.mine" }),
			scaleClass = "Medium",
			collisionPolicy = "Simple",
			streamingGroupId = "main_world.resources",
			qualityTiers = table.freeze({ "Minimum", "Reduced", "Full" }),
			sockets = table.freeze({}),
			sourceModelPath = "Environment/Industry/OreCartA",
			footprintStuds = table.freeze({ x = 9, y = 6, z = 6 }),
			presentationOnly = true,
			gameplayAuthorityOwnerId = nil,
		}),
		table.freeze({
			id = "env.prop.industry.timber_barricade_a",
			family = "Prop",
			variantId = "mine.timber_barricade.a",
			biomeIds = table.freeze({ "biome.abandoned_industry", "biome.mine" }),
			scaleClass = "Medium",
			collisionPolicy = "Blocker",
			streamingGroupId = "main_world.resources",
			qualityTiers = table.freeze({ "Minimum", "Reduced", "Full" }),
			sockets = table.freeze({}),
			sourceModelPath = "Environment/Industry/TimberBarricadeA",
			footprintStuds = table.freeze({ x = 12, y = 7, z = 3 }),
			presentationOnly = true,
			gameplayAuthorityOwnerId = nil,
		}),
		table.freeze({
			id = "env.camp.frontier.abandoned_fire_ring_a",
			family = "Camp",
			variantId = "frontier.abandoned_fire_ring.a",
			biomeIds = table.freeze({ "biome.frontier", "biome.corrupted_wilderness" }),
			scaleClass = "Medium",
			collisionPolicy = "None",
			streamingGroupId = "main_world.resources",
			qualityTiers = table.freeze({ "Minimum", "Reduced", "Full" }),
			sockets = table.freeze({
				table.freeze({ id = "socket.vfx.embers", kind = "VFX" }),
				table.freeze({ id = "socket.audio.fire", kind = "Audio" }),
			}),
			sourceModelPath = "Environment/Camps/AbandonedFireRingA",
			footprintStuds = table.freeze({ x = 8, y = 2, z = 8 }),
			presentationOnly = true,
			gameplayAuthorityOwnerId = nil,
		}),
		table.freeze({
			id = "env.camp.frontier.torn_shelter_a",
			family = "Camp",
			variantId = "frontier.torn_shelter.a",
			biomeIds = table.freeze({ "biome.frontier", "biome.corrupted_wilderness" }),
			scaleClass = "Large",
			collisionPolicy = "Simple",
			streamingGroupId = "main_world.structures",
			qualityTiers = table.freeze({ "Minimum", "Reduced", "Full" }),
			sockets = table.freeze({}),
			sourceModelPath = "Environment/Camps/TornShelterA",
			footprintStuds = table.freeze({ x = 12, y = 8, z = 8 }),
			presentationOnly = true,
			gameplayAuthorityOwnerId = nil,
		}),
		table.freeze({
			id = "env.prop.frontier.supply_crate_stack_a",
			family = "Prop",
			variantId = "frontier.supply_crate_stack.a",
			biomeIds = table.freeze({ "biome.frontier", "biome.abandoned_industry" }),
			scaleClass = "Medium",
			collisionPolicy = "Simple",
			streamingGroupId = "main_world.resources",
			qualityTiers = table.freeze({ "Minimum", "Reduced", "Full" }),
			sockets = table.freeze({}),
			sourceModelPath = "Environment/Camps/SupplyCrateStackA",
			footprintStuds = table.freeze({ x = 6, y = 6, z = 5 }),
			presentationOnly = true,
			gameplayAuthorityOwnerId = nil,
		}),
		table.freeze({
			id = "env.camp.frontier.hanging_tarp_frame_a",
			family = "Camp",
			variantId = "frontier.hanging_tarp_frame.a",
			biomeIds = table.freeze({ "biome.frontier", "biome.corrupted_wilderness" }),
			scaleClass = "Large",
			collisionPolicy = "Simple",
			streamingGroupId = "main_world.structures",
			qualityTiers = table.freeze({ "Minimum", "Reduced", "Full" }),
			sockets = table.freeze({}),
			sourceModelPath = "Environment/Camps/HangingTarpFrameA",
			footprintStuds = table.freeze({ x = 9, y = 7, z = 2 }),
			presentationOnly = true,
			gameplayAuthorityOwnerId = nil,
		}),
		table.freeze({
			id = "env.structure.frontier.collapsed_shack_corner_a",
			family = "Structure",
			variantId = "frontier.collapsed_shack_corner.a",
			biomeIds = table.freeze({ "biome.frontier", "biome.corrupted_wilderness" }),
			scaleClass = "Large",
			collisionPolicy = "Blocker",
			streamingGroupId = "main_world.structures",
			qualityTiers = table.freeze({ "Minimum", "Reduced", "Full" }),
			sockets = table.freeze({}),
			sourceModelPath = "Environment/Structures/CollapsedShackCornerA",
			footprintStuds = table.freeze({ x = 9, y = 10, z = 9 }),
			presentationOnly = true,
			gameplayAuthorityOwnerId = nil,
		}),
		table.freeze({
			id = "env.structure.frontier.boarded_doorway_a",
			family = "Structure",
			variantId = "frontier.boarded_doorway.a",
			biomeIds = table.freeze({ "biome.frontier", "biome.abandoned_industry" }),
			scaleClass = "Large",
			collisionPolicy = "Blocker",
			streamingGroupId = "main_world.structures",
			qualityTiers = table.freeze({ "Minimum", "Reduced", "Full" }),
			sockets = table.freeze({}),
			sourceModelPath = "Environment/Structures/BoardedDoorwayA",
			footprintStuds = table.freeze({ x = 7, y = 9, z = 1 }),
			presentationOnly = true,
			gameplayAuthorityOwnerId = nil,
		}),
		table.freeze({
			id = "env.structure.frontier.broken_roof_frame_a",
			family = "Structure",
			variantId = "frontier.broken_roof_frame.a",
			biomeIds = table.freeze({ "biome.frontier", "biome.corrupted_wilderness" }),
			scaleClass = "Large",
			collisionPolicy = "Simple",
			streamingGroupId = "main_world.structures",
			qualityTiers = table.freeze({ "Minimum", "Reduced", "Full" }),
			sockets = table.freeze({}),
			sourceModelPath = "Environment/Structures/BrokenRoofFrameA",
			footprintStuds = table.freeze({ x = 12, y = 9, z = 7 }),
			presentationOnly = true,
			gameplayAuthorityOwnerId = nil,
		}),
		table.freeze({
			id = "env.structure.frontier.ruined_window_wall_a",
			family = "Structure",
			variantId = "frontier.ruined_window_wall.a",
			biomeIds = table.freeze({ "biome.frontier", "biome.abandoned_industry" }),
			scaleClass = "Large",
			collisionPolicy = "Blocker",
			streamingGroupId = "main_world.structures",
			qualityTiers = table.freeze({ "Minimum", "Reduced", "Full" }),
			sockets = table.freeze({}),
			sourceModelPath = "Environment/Structures/RuinedWindowWallA",
			footprintStuds = table.freeze({ x = 12, y = 9, z = 1 }),
			presentationOnly = true,
			gameplayAuthorityOwnerId = nil,
		}),
	}),
}

return table.freeze(Registry)
