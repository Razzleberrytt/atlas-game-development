--!strict

-- Additive held-source registry shard for visual-production wave 2.
-- These definitions compose with EnvironmentAssetKitRegistry v4 under the
-- same EnvironmentAssetKitContracts. They remain presentation-only/unmapped.

local Definitions = table.freeze({
	table.freeze({
		id = "env.structure.industry.rail_track_section_a",
		family = "Industrial",
		variantId = "rail.track_section.a",
		biomeIds = table.freeze({ "biome.abandoned_industry", "biome.mine" }),
		scaleClass = "Large",
		collisionPolicy = "Simple",
		streamingGroupId = "main_world.structures",
		qualityTiers = table.freeze({ "Minimum", "Reduced", "Full" }),
		sockets = table.freeze({}),
		sourceModelPath = "Environment/Industry/RailTrackSectionA",
		footprintStuds = table.freeze({ x = 6, y = 1, z = 16 }),
		presentationOnly = true,
		gameplayAuthorityOwnerId = nil,
	}),
	table.freeze({
		id = "env.structure.industry.broken_rail_bend_a",
		family = "Industrial",
		variantId = "rail.broken_bend.a",
		biomeIds = table.freeze({ "biome.abandoned_industry", "biome.mine" }),
		scaleClass = "Large",
		collisionPolicy = "Simple",
		streamingGroupId = "main_world.structures",
		qualityTiers = table.freeze({ "Minimum", "Reduced", "Full" }),
		sockets = table.freeze({}),
		sourceModelPath = "Environment/Industry/BrokenRailBendA",
		footprintStuds = table.freeze({ x = 7, y = 2, z = 10 }),
		presentationOnly = true,
		gameplayAuthorityOwnerId = nil,
	}),
	table.freeze({
		id = "env.prop.industry.signal_post_a",
		family = "Prop",
		variantId = "rail.signal_post.a",
		biomeIds = table.freeze({ "biome.abandoned_industry", "biome.mine" }),
		scaleClass = "Large",
		collisionPolicy = "Simple",
		streamingGroupId = "main_world.resources",
		qualityTiers = table.freeze({ "Minimum", "Reduced", "Full" }),
		sockets = table.freeze({ table.freeze({ id = "socket.vfx.signal", kind = "VFX" }) }),
		sourceModelPath = "Environment/Industry/SignalPostA",
		footprintStuds = table.freeze({ x = 4, y = 9, z = 4 }),
		presentationOnly = true,
		gameplayAuthorityOwnerId = nil,
	}),
	table.freeze({
		id = "env.prop.industry.collapsed_pipe_stack_a",
		family = "Prop",
		variantId = "industry.collapsed_pipe_stack.a",
		biomeIds = table.freeze({ "biome.abandoned_industry", "biome.mine" }),
		scaleClass = "Large",
		collisionPolicy = "Simple",
		streamingGroupId = "main_world.resources",
		qualityTiers = table.freeze({ "Minimum", "Reduced", "Full" }),
		sockets = table.freeze({}),
		sourceModelPath = "Environment/Industry/CollapsedPipeStackA",
		footprintStuds = table.freeze({ x = 11, y = 5, z = 8 }),
		presentationOnly = true,
		gameplayAuthorityOwnerId = nil,
	}),
	table.freeze({
		id = "env.growth.corruption.resin_cluster_a",
		family = "GroundClutter",
		variantId = "corruption.resin_cluster.a",
		biomeIds = table.freeze({ "biome.corrupted_wilderness", "biome.abandoned_industry" }),
		scaleClass = "Medium",
		collisionPolicy = "None",
		streamingGroupId = "main_world.resources",
		qualityTiers = table.freeze({ "Minimum", "Reduced", "Full" }),
		sockets = table.freeze({ table.freeze({ id = "socket.vfx.resin", kind = "VFX" }) }),
		sourceModelPath = "Environment/Corruption/ResinGrowthClusterA",
		footprintStuds = table.freeze({ x = 6, y = 4, z = 6 }),
		presentationOnly = true,
		gameplayAuthorityOwnerId = nil,
	}),
	table.freeze({
		id = "env.growth.corruption.root_arch_a",
		family = "Root",
		variantId = "corruption.root_arch.a",
		biomeIds = table.freeze({ "biome.corrupted_wilderness" }),
		scaleClass = "Large",
		collisionPolicy = "Blocker",
		streamingGroupId = "main_world.structures",
		qualityTiers = table.freeze({ "Minimum", "Reduced", "Full" }),
		sockets = table.freeze({}),
		sourceModelPath = "Environment/Corruption/CorruptedRootArchA",
		footprintStuds = table.freeze({ x = 8, y = 7, z = 3 }),
		presentationOnly = true,
		gameplayAuthorityOwnerId = nil,
	}),
	table.freeze({
		id = "env.growth.corruption.ground_spire_a",
		family = "GroundClutter",
		variantId = "corruption.ground_spire.a",
		biomeIds = table.freeze({ "biome.corrupted_wilderness", "biome.abandoned_industry" }),
		scaleClass = "Large",
		collisionPolicy = "Simple",
		streamingGroupId = "main_world.resources",
		qualityTiers = table.freeze({ "Minimum", "Reduced", "Full" }),
		sockets = table.freeze({ table.freeze({ id = "socket.vfx.spire", kind = "VFX" }) }),
		sourceModelPath = "Environment/Corruption/CorruptionGroundSpireA",
		footprintStuds = table.freeze({ x = 5, y = 7, z = 5 }),
		presentationOnly = true,
		gameplayAuthorityOwnerId = nil,
	}),
	table.freeze({
		id = "env.growth.corruption.spore_lantern_a",
		family = "GroundClutter",
		variantId = "corruption.spore_lantern.a",
		biomeIds = table.freeze({ "biome.corrupted_wilderness", "biome.mine" }),
		scaleClass = "Medium",
		collisionPolicy = "None",
		streamingGroupId = "main_world.resources",
		qualityTiers = table.freeze({ "Minimum", "Reduced", "Full" }),
		sockets = table.freeze({
			table.freeze({ id = "socket.vfx.spores", kind = "VFX" }),
			table.freeze({ id = "socket.audio.hum", kind = "Audio" }),
		}),
		sourceModelPath = "Environment/Corruption/SporeLanternGrowthA",
		footprintStuds = table.freeze({ x = 4, y = 6, z = 4 }),
		presentationOnly = true,
		gameplayAuthorityOwnerId = nil,
	}),
})

return table.freeze({
	Version = "ENV-ASSET-KIT-WAVE2-v2",
	Definitions = Definitions,
})
