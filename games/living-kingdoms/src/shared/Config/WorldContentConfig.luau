--!strict

-- BA-004: stable world-content registry.
--
-- This registry is the bridge between the modern source-managed runtime and
-- names recovered from the original Studio place. It does NOT activate legacy
-- services or make imported instance names authoritative.

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local WorldContentContracts = require(ReplicatedStorage.Shared.World.WorldContentContracts)

type ContentDescriptor = WorldContentContracts.ContentDescriptor

local descriptors: { ContentDescriptor } = {
	table.freeze({
		id = "world.operation.forest",
		kind = "world",
		displayName = "Living Kingdoms Operation World",
		legacyAliases = table.freeze({ "LivingKingdomsWorld" }),
		active = true,
		notes = "Current authoritative operation world generated by WorldFoundationService.",
	}),
	table.freeze({
		id = "world.hub.primary",
		kind = "zone",
		displayName = "Hub Town",
		legacyAliases = table.freeze({ "HubTown" }),
		active = false,
		notes = "Preserved Studio-authored hub; pending reconciliation into current preparation flow.",
	}),
	table.freeze({
		id = "route.world.primary",
		kind = "route",
		displayName = "Primary World Path",
		legacyAliases = table.freeze({ "WorldPath" }),
		active = false,
		notes = "Imported world route identity preserved for future geometry reconciliation.",
	}),
	table.freeze({
		id = "zone.resources.primary",
		kind = "zone",
		displayName = "Resource Region",
		legacyAliases = table.freeze({ "Resources" }),
		active = false,
	}),
	table.freeze({
		id = "zone.structures.primary",
		kind = "zone",
		displayName = "World Structures",
		legacyAliases = table.freeze({ "WorldStructures", "Buildings" }),
		active = false,
	}),
	table.freeze({
		id = "spawn.operation.insertion",
		kind = "spawn",
		displayName = "Operation Insertion",
		legacyAliases = table.freeze({ "SpawnLocation", "BootstrapSafetySpawn" }),
		active = true,
	}),
	table.freeze({
		id = "resource.tree",
		kind = "resource",
		displayName = "Tree",
		legacyAliases = table.freeze({ "Trees" }),
		active = false,
	}),
	table.freeze({
		id = "resource.rock",
		kind = "resource",
		displayName = "Rock",
		legacyAliases = table.freeze({ "Rocks" }),
		active = false,
	}),
	table.freeze({
		id = "resource.iron_ore",
		kind = "resource",
		displayName = "Iron Ore",
		legacyAliases = table.freeze({ "IronOre" }),
		active = false,
	}),
	table.freeze({
		id = "landmark.ranger_station",
		kind = "landmark",
		displayName = "Ranger Station",
		legacyAliases = table.freeze({ "RangerStation" }),
		active = true,
	}),
	table.freeze({
		id = "landmark.logging_road",
		kind = "landmark",
		displayName = "Logging Road",
		legacyAliases = table.freeze({ "LoggingRoad" }),
		active = true,
	}),
	table.freeze({
		id = "landmark.lookout_tower",
		kind = "landmark",
		displayName = "Forest Service Lookout",
		legacyAliases = table.freeze({ "LookoutTower" }),
		active = true,
	}),
	table.freeze({
		id = "landmark.campground",
		kind = "landmark",
		displayName = "Campground",
		legacyAliases = table.freeze({ "Campground" }),
		active = true,
	}),
	table.freeze({
		id = "landmark.creek_crossing",
		kind = "landmark",
		displayName = "Creek Crossing",
		legacyAliases = table.freeze({ "CreekCrossing" }),
		active = true,
	}),
	table.freeze({
		id = "landmark.rocky_overlook",
		kind = "landmark",
		displayName = "Rocky Overlook",
		legacyAliases = table.freeze({ "RockyOverlook" }),
		active = true,
	}),
	table.freeze({
		id = "landmark.military_roadblock",
		kind = "landmark",
		displayName = "Military Roadblock",
		legacyAliases = table.freeze({ "MilitaryRoadblock" }),
		active = true,
	}),
	table.freeze({
		id = "landmark.extraction_clearing",
		kind = "landmark",
		displayName = "Forest Extraction Clearing",
		legacyAliases = table.freeze({ "ExtractionClearing" }),
		active = true,
	}),
}

local byId = WorldContentContracts.indexById(descriptors)
local legacyAliases = WorldContentContracts.indexLegacyAliases(descriptors)

return table.freeze({
	Version = "BA-004-v1",
	Descriptors = table.freeze(descriptors),
	ById = table.freeze(byId),
	LegacyAliases = table.freeze(legacyAliases),
})
