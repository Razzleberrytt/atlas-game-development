--!strict

local Definitions = table.freeze({
	table.freeze({
		id = "env.prop.industry.hanging_work_lamp_a",
		family = "Prop",
		variantId = "industry.utility.hanging_work_lamp.a",
		biomeIds = table.freeze({ "biome.abandoned_industry", "biome.mine" }),
		scaleClass = "Small",
		collisionPolicy = "None",
		streamingGroupId = "main_world.resources",
		qualityTiers = table.freeze({ "Minimum", "Reduced", "Full" }),
		sockets = table.freeze({ table.freeze({ id = "socket.vfx.light", kind = "VFX" }) }),
		sourceModelPath = "Environment/Utility/HangingWorkLampA",
		footprintStuds = table.freeze({ x = 2, y = 7, z = 2 }),
		presentationOnly = true,
		gameplayAuthorityOwnerId = nil,
	}),
	table.freeze({
		id = "env.prop.industry.broken_floodlight_a",
		family = "Prop",
		variantId = "industry.utility.broken_floodlight.a",
		biomeIds = table.freeze({ "biome.abandoned_industry", "biome.frontier" }),
		scaleClass = "Large",
		collisionPolicy = "None",
		streamingGroupId = "main_world.resources",
		qualityTiers = table.freeze({ "Minimum", "Reduced", "Full" }),
		sockets = table.freeze({ table.freeze({ id = "socket.vfx.light", kind = "VFX" }) }),
		sourceModelPath = "Environment/Utility/BrokenFloodlightA",
		footprintStuds = table.freeze({ x = 4, y = 8, z = 4 }),
		presentationOnly = true,
		gameplayAuthorityOwnerId = nil,
	}),
	table.freeze({
		id = "env.prop.industry.generator_shell_a",
		family = "Prop",
		variantId = "industry.utility.generator_shell.a",
		biomeIds = table.freeze({ "biome.abandoned_industry", "biome.frontier", "biome.mine" }),
		scaleClass = "Medium",
		collisionPolicy = "None",
		streamingGroupId = "main_world.resources",
		qualityTiers = table.freeze({ "Minimum", "Reduced", "Full" }),
		sockets = table.freeze({
			table.freeze({ id = "socket.audio.engine", kind = "Audio" }),
			table.freeze({ id = "socket.vfx.exhaust", kind = "VFX" }),
		}),
		sourceModelPath = "Environment/Utility/GeneratorShellA",
		footprintStuds = table.freeze({ x = 5, y = 4, z = 3 }),
		presentationOnly = true,
		gameplayAuthorityOwnerId = nil,
	}),
	table.freeze({
		id = "env.prop.frontier.lantern_post_a",
		family = "Prop",
		variantId = "frontier.utility.lantern_post.a",
		biomeIds = table.freeze({ "biome.frontier", "biome.corrupted_wilderness" }),
		scaleClass = "Medium",
		collisionPolicy = "None",
		streamingGroupId = "main_world.resources",
		qualityTiers = table.freeze({ "Minimum", "Reduced", "Full" }),
		sockets = table.freeze({ table.freeze({ id = "socket.vfx.light", kind = "VFX" }) }),
		sourceModelPath = "Environment/Utility/LanternPostA",
		footprintStuds = table.freeze({ x = 3, y = 7, z = 3 }),
		presentationOnly = true,
		gameplayAuthorityOwnerId = nil,
	}),
})

return table.freeze({ Version = "ENV-ASSET-KIT-WAVE6-v1", Definitions = Definitions })
