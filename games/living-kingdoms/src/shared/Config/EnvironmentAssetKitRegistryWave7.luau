--!strict

local Definitions = table.freeze({
	table.freeze({
		id = "env.tree.corrupted.dead_pine_b",
		family = "DeadTree",
		variantId = "corrupted.dead_pine.b",
		biomeIds = table.freeze({ "biome.corrupted_wilderness", "biome.frontier" }),
		scaleClass = "Hero",
		collisionPolicy = "None",
		streamingGroupId = "main_world.resources",
		qualityTiers = table.freeze({ "Minimum", "Reduced", "Full" }),
		sockets = table.freeze({ table.freeze({ id = "socket.audio.creak", kind = "Audio" }) }),
		sourceModelPath = "Environment/Foliage/DeadPineB",
		footprintStuds = table.freeze({ x = 8, y = 23, z = 8 }),
		presentationOnly = true,
		gameplayAuthorityOwnerId = nil,
	}),
	table.freeze({
		id = "env.rock.frontier.boulder_cluster_b",
		family = "Rock",
		variantId = "frontier.boulder_cluster.b",
		biomeIds = table.freeze({ "biome.frontier", "biome.corrupted_wilderness", "biome.mine" }),
		scaleClass = "Large",
		collisionPolicy = "None",
		streamingGroupId = "main_world.resources",
		qualityTiers = table.freeze({ "Minimum", "Reduced", "Full" }),
		sockets = table.freeze({}),
		sourceModelPath = "Environment/Rocks/BoulderClusterB",
		footprintStuds = table.freeze({ x = 11, y = 5, z = 8 }),
		presentationOnly = true,
		gameplayAuthorityOwnerId = nil,
	}),
	table.freeze({
		id = "env.root.corrupted.twisted_b",
		family = "Root",
		variantId = "corrupted.twisted_root.b",
		biomeIds = table.freeze({ "biome.corrupted_wilderness" }),
		scaleClass = "Medium",
		collisionPolicy = "None",
		streamingGroupId = "main_world.resources",
		qualityTiers = table.freeze({ "Minimum", "Reduced", "Full" }),
		sockets = table.freeze({}),
		sourceModelPath = "Environment/Foliage/TwistedRootB",
		footprintStuds = table.freeze({ x = 8, y = 5, z = 7 }),
		presentationOnly = true,
		gameplayAuthorityOwnerId = nil,
	}),
	table.freeze({
		id = "env.brush.frontier.snag_b",
		family = "Brush",
		variantId = "frontier.brush_snag.b",
		biomeIds = table.freeze({ "biome.frontier", "biome.corrupted_wilderness" }),
		scaleClass = "Small",
		collisionPolicy = "None",
		streamingGroupId = "main_world.resources",
		qualityTiers = table.freeze({ "Minimum", "Reduced", "Full" }),
		sockets = table.freeze({}),
		sourceModelPath = "Environment/Foliage/BrushSnagB",
		footprintStuds = table.freeze({ x = 4, y = 4, z = 3 }),
		presentationOnly = true,
		gameplayAuthorityOwnerId = nil,
	}),
})

return table.freeze({ Version = "ENV-ASSET-KIT-WAVE7-v1", Definitions = Definitions })
