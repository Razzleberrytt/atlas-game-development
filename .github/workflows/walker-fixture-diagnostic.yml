name: Walker fixture diagnostic

on:
  pull_request:

permissions:
  contents: read

jobs:
  fixtures-p0-p4:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: CompeyDev/setup-rokit@v0.1.2
        with: { version: v1.2.0, path: ., cache: false, token: "${{ github.token }}" }
      - shell: bash
        run: |
          set -euo pipefail
          while IFS= read -r -d '' fixture; do base="$(basename "$fixture")"; if [[ "$base" =~ ^[Pp][0-4] ]]; then echo "Running $fixture"; lune run "$fixture"; fi; done < <(find games/living-kingdoms/tests -type f -name '*.test.luau' -print0 | sort -z)

  fixtures-p5:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: CompeyDev/setup-rokit@v0.1.2
        with: { version: v1.2.0, path: ., cache: false, token: "${{ github.token }}" }
      - shell: bash
        run: |
          set -euo pipefail
          while IFS= read -r -d '' fixture; do base="$(basename "$fixture")"; if [[ "$base" =~ ^[Pp]5 ]]; then echo "Running $fixture"; lune run "$fixture"; fi; done < <(find games/living-kingdoms/tests -type f -name '*.test.luau' -print0 | sort -z)

  fixtures-p6:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: CompeyDev/setup-rokit@v0.1.2
        with: { version: v1.2.0, path: ., cache: false, token: "${{ github.token }}" }
      - shell: bash
        run: |
          set -euo pipefail
          while IFS= read -r -d '' fixture; do base="$(basename "$fixture")"; if [[ "$base" =~ ^[Pp]6 ]]; then echo "Running $fixture"; lune run "$fixture"; fi; done < <(find games/living-kingdoms/tests -type f -name '*.test.luau' -print0 | sort -z)

  fixtures-p7:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: CompeyDev/setup-rokit@v0.1.2
        with: { version: v1.2.0, path: ., cache: false, token: "${{ github.token }}" }
      - shell: bash
        run: |
          set -euo pipefail
          while IFS= read -r -d '' fixture; do base="$(basename "$fixture")"; if [[ "$base" =~ ^[Pp]7 ]]; then echo "Running $fixture"; lune run "$fixture"; fi; done < <(find games/living-kingdoms/tests -type f -name '*.test.luau' -print0 | sort -z)

  fixtures-p8:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: CompeyDev/setup-rokit@v0.1.2
        with: { version: v1.2.0, path: ., cache: false, token: "${{ github.token }}" }
      - shell: bash
        run: |
          set -euo pipefail
          while IFS= read -r -d '' fixture; do base="$(basename "$fixture")"; if [[ "$base" =~ ^[Pp]8 ]]; then echo "Running $fixture"; lune run "$fixture"; fi; done < <(find games/living-kingdoms/tests -type f -name '*.test.luau' -print0 | sort -z)

  fixtures-p9:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: CompeyDev/setup-rokit@v0.1.2
        with: { version: v1.2.0, path: ., cache: false, token: "${{ github.token }}" }
      - shell: bash
        run: |
          set -euo pipefail
          while IFS= read -r -d '' fixture; do base="$(basename "$fixture")"; if [[ "$base" =~ ^[Pp]9 ]]; then echo "Running $fixture"; lune run "$fixture"; fi; done < <(find games/living-kingdoms/tests -type f -name '*.test.luau' -print0 | sort -z)
