name: Exclusion Walker windup apply

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-22.04
    steps:
      - run: echo "Windup implementation is routed through the trusted validation workflow."
