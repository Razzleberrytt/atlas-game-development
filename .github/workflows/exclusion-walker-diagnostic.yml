name: Exclusion Walker diagnostic

on:
  pull_request:

permissions:
  contents: read

jobs:
  fixtures-g:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: CompeyDev/setup-rokit@v0.1.2
        with:
          version: v1.2.0
          path: .
          cache: false
          token: ${{ github.token }}
      - shell: bash
        run: |
          set -euo pipefail
          while IFS= read -r -d '' fixture; do
            base="$(basename "$fixture")"
            if [[ "$base" =~ ^[Gg] ]]; then echo "Running $fixture"; lune run "$fixture"; fi
          done < <(find games/living-kingdoms/tests -type f -name '*.test.luau' -print0 | sort -z)

  fixtures-h:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: CompeyDev/setup-rokit@v0.1.2
        with:
          version: v1.2.0
          path: .
          cache: false
          token: ${{ github.token }}
      - shell: bash
        run: |
          set -euo pipefail
          while IFS= read -r -d '' fixture; do
            base="$(basename "$fixture")"
            if [[ "$base" =~ ^[Hh] ]]; then echo "Running $fixture"; lune run "$fixture"; fi
          done < <(find games/living-kingdoms/tests -type f -name '*.test.luau' -print0 | sort -z)

  fixtures-i:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: CompeyDev/setup-rokit@v0.1.2
        with:
          version: v1.2.0
          path: .
          cache: false
          token: ${{ github.token }}
      - shell: bash
        run: |
          set -euo pipefail
          while IFS= read -r -d '' fixture; do
            base="$(basename "$fixture")"
            if [[ "$base" =~ ^[Ii] ]]; then echo "Running $fixture"; lune run "$fixture"; fi
          done < <(find games/living-kingdoms/tests -type f -name '*.test.luau' -print0 | sort -z)

  fixtures-j:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: CompeyDev/setup-rokit@v0.1.2
        with:
          version: v1.2.0
          path: .
          cache: false
          token: ${{ github.token }}
      - shell: bash
        run: |
          set -euo pipefail
          while IFS= read -r -d '' fixture; do
            base="$(basename "$fixture")"
            if [[ "$base" =~ ^[Jj] ]]; then echo "Running $fixture"; lune run "$fixture"; fi
          done < <(find games/living-kingdoms/tests -type f -name '*.test.luau' -print0 | sort -z)

  fixtures-k:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: CompeyDev/setup-rokit@v0.1.2
        with:
          version: v1.2.0
          path: .
          cache: false
          token: ${{ github.token }}
      - shell: bash
        run: |
          set -euo pipefail
          while IFS= read -r -d '' fixture; do
            base="$(basename "$fixture")"
            if [[ "$base" =~ ^[Kk] ]]; then echo "Running $fixture"; lune run "$fixture"; fi
          done < <(find games/living-kingdoms/tests -type f -name '*.test.luau' -print0 | sort -z)

  fixtures-l:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: CompeyDev/setup-rokit@v0.1.2
        with:
          version: v1.2.0
          path: .
          cache: false
          token: ${{ github.token }}
      - shell: bash
        run: |
          set -euo pipefail
          while IFS= read -r -d '' fixture; do
            base="$(basename "$fixture")"
            if [[ "$base" =~ ^[Ll] ]]; then echo "Running $fixture"; lune run "$fixture"; fi
          done < <(find games/living-kingdoms/tests -type f -name '*.test.luau' -print0 | sort -z)
