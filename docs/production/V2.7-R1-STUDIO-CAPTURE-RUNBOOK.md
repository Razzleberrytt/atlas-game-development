# Blueprint v2.7 R1 Studio Capture Runbook

This runbook minimizes manual work for the first accepted R1 runtime packet. It does **not** change the build under test.

## Canonical build under test

Use exactly:

- source/build commit: `c55287fac4ecefc120c541958a6a06049b0a78cd` (GitHub's PR #245 synthetic merge of fix commit `60eba47d766529e52d8b1239da17397deeaa7de9` into `26898b21bffd8e4b50001da2e3812e17760bab6a`)
- artifact: `living-kingdoms-rbxlx-c55287fac4ecefc120c541958a6a06049b0a78cd`
- GitHub workflow run: `31282591558`
- GitHub artifact ID: `9028866465`
- artifact digest: `sha256:4cdb778a37e22c9172216d999a2dfcffec75e05e29baffaad2233a2c5dbf92c5`
- extracted `LivingKingdoms.rbxlx` SHA-256: `62200adf721685b1d6537d03e6914aaac5d789b21ed00405dac47421931db0d1`
- recorded: 2026-08-08

Do not substitute a different artifact for the recorded R1 acceptance run.

## Why the helper is safe

`games/living-kingdoms/tools/studio/V27R1Capture.client.luau` lives outside `games/living-kingdoms/src/` and is therefore not part of the Rojo place mapping. It only reads existing attributes and Highlight instances. It does not create, destroy, enable, disable, mutate, or send anything.

## One-player R1 procedure

1. Open the exact canonical artifact in Roblox Studio.
2. Confirm the place is the R1 build identified above.
3. Clear Studio Output.
4. Start one-player Play from a cold client start.
5. In the running **client** context, run the complete contents of `games/living-kingdoms/tools/studio/V27R1Capture.client.luau` from the Command Bar once shortly after the client becomes controllable.
6. Keep playing for at least 60 seconds so normal horde-state publishing occurs. Trigger ordinary enemy hit/kill feedback during the run if possible.
7. Run the same capture script again in the client context.
8. In Output, search for:
   - `HordeNetwork.State`
   - `invocation`
   - `queue`
   - `discard`
   - `[Living Kingdoms] Disabled broad Highlight`
9. Record whether ordinary narrow enemy hit/kill Highlight feedback remained visible and whether any broad blue/yellow world wash appeared.
10. Stop play before changing any source or rollout flag.

## Capture output

Each invocation prints one machine-readable line:

```text
LK_V27_R1_CAPTURE {json}
```

It also prints:

```text
LK_V27_R1_CAPTURE_SNAPSHOT_PASSABLE true|false
```

`SNAPSHOT_PASSABLE` checks only facts available to the capture script:

- early listener bound;
- received message count greater than zero;
- invalid message count zero;
- numeric last revision;
- broad Highlight guard active;
- no still-enabled broad Highlight target matching the R1 guard definition.

It **does not** declare R1 accepted. Queue/discard warnings, startup regressions, visible behavior, exact build identity, and legitimate narrow Highlight behavior still require the operator/runtime observations in the evidence packet.

## Fields captured automatically

The JSON record includes:

```text
listener.bound
listener.messagesReceived
listener.invalidMessages
listener.lastRevision
highlightGuard.active
highlightGuard.rejectedCount
highlightGuard.lastTarget
highlightScan.total
highlightScan.enabled
highlightScan.rejectedInstances[]
highlightScan.stillEnabledBroad[]
```

`highlightScan.stillEnabledBroad` independently applies the same conservative R1 broad-target definition to every enabled Highlight currently under Workspace. A non-empty array is a failure lead and must not be hidden by the guard counters.

## What to paste back into the evidence packet

Keep both `LK_V27_R1_CAPTURE {json}` lines: the first cold-start snapshot and the second 60-second snapshot. Also record:

```text
queue/discard warning count =
narrow enemy Highlight works = yes/no
broad world wash appears = yes/no
startup regression = yes/no
exact disabled-broad-Highlight warning lines =
other new client errors =
Roblox Studio version =
run date/time =
```

Those facts are sufficient to fill the R1 packet without hand-transcribing every attribute.

## Deterministic acceptance evaluator

After the run, `games/living-kingdoms/tools/evidence/V27R1EvidenceEvaluator.luau` applies the R1 acceptance rules deterministically. It is tooling only and is covered by a Lune fixture.

The evaluator returns exactly one of:

```text
PASS     complete accepted evidence
FAIL     complete evidence with one or more failed R1 gates
PARTIAL  required runtime observations are still missing
INVALID  wrong canonical artifact/schema or malformed evidence
```

Use `docs/production/evidence/V27-R1-EVALUATOR-INPUT.template.json` as the handoff shape. Replace `firstCapture` and `finalCapture` with the two JSON objects printed by the Studio helper and fill the five manual observations.

The evaluator deliberately invalidates evidence if the source commit, artifact ID, artifact digest, or capture schema does not match the canonical R1 run. A valid `PASS` also requires the final message count to be positive and greater than the first capture, no revision regression, zero invalid messages, an active broad-Highlight guard, an empty final `stillEnabledBroad` list, zero queue/discard warnings, no startup regression, working narrow enemy Highlight feedback, no broad world wash, and zero new client errors attributable to R1.

## Acceptance boundary

Do not merge draft PR #221 or activate R2 from stacked PR #222 merely because the capture script reports `SNAPSHOT_PASSABLE true`.

R1 acceptance still requires the full checklist in:

`docs/production/evidence/2026-08-07-336-r1-state-highlight-containment.md`

If R1 passes, update the cutover ledger with measured runtime facts and only then promote the listener-consolidation stage.
